// ACCENT is the one brand color used across every screen's buttons, active
// states, and highlights — pulled from the real app icon (Teal Soundwave
// Vinyl, assets/icon.png), replacing an unrelated indigo (#6366f1) that
// every screen previously hardcoded independently. Semantic colors (the
// storage screen's LRU-cache green, destructive-action red) stay separate
// from this — see /codebase-design's "semantic color isn't your accent".
export const ACCENT = '#2dd9c4'
export const ACCENT_LIGHT = '#7ce7da'
export const ACCENT_TINT_20 = 'rgba(45, 217, 196, 0.2)'
